Flask==3.0.3
numpy>=1.23.0,<1.24.5  # Ensure compatibility with many scientific libraries
pandas>=1.3.0,<2.0.0   # Stable version for working with large data
scikit-learn>=1.0,<1.3.3  # Machine learning toolkit used for your models
scipy>=1.8.0,<1.10.0  # For scientific computing
Werkzeug>=2.3.6,<3.0.0  # Flask dependency
requests>=2.28.0,<2.32.0  # HTTP request library
urllib3>=1.26,<2.1  # Version to avoid issues with requests/boto3

# Orange3 (core library)
Orange3>=3.30.0,<4.0.0  # For compatibility, avoid latest beta versions of Orange
# Dependencies Orange3 uses (but we can pin key ones)
openpyxl>=3.0.9,<3.2.0  # For Excel file reading in Orange
pyyaml>=5.4,<6.1  # YAML library
bottleneck>=1.3,<1.4.1  # Performance optimization in numpy arrays
keyring>=21.5,<25.5.0  # For securely managing keys

# PyTorch for CPU (remove GPU/CUDA libs)
torch>=1.12.0,<2.0  # Compatible with older systems and Orange
torchvision>=0.13.0,<0.15.0  # Use appropriate torchvision for PyTorch compatibility

# Gunicorn for production WSGI server
gunicorn>=20.0,<21.0
